services:
  network-holder:
    image: alpine:3.22
    container_name: network_holder
    command: ["tail", "-f", "/dev/null"]
    restart: unless-stopped
    networks:
      - microservices-net

networks:
  microservices-net:
    name: microservices-net
